#include "std_lib_facilities.h"

int main(){ 					// 'Main' isn't recognized by the compiler, but 'main' is.

	string s = "Goodbye, cruel world!";     // 'STRING' isn't recognized by the compiler, but 'string' it is.
	
	cout << s << '\n'; 			// 'cOut' and 'S' isn't recognized by the compiler, but 'cout' and 's' is. 

return 0;
}
